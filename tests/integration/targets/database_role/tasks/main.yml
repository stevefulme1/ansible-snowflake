---
# Integration tests for stevefulme1.snowflake.database_role
# Requires: SNOWFLAKE_ACCOUNT, SNOWFLAKE_USER, SNOWFLAKE_PRIVATE_KEY

- name: Set test resource name
  ansible.builtin.set_fact:
    test_name: "ANSIBLE_TEST_{{ lookup('password', '/dev/null chars=ascii_uppercase length=6') }}"
    test_db: "ANSIBLE_TEST_DB_{{ lookup('password', '/dev/null chars=ascii_uppercase length=6') }}"

- name: Create test database
  stevefulme1.snowflake.database:
    name: "{{ test_db }}"
    account: "{{ snowflake_account }}"
    user: "{{ snowflake_user }}"
    private_key: "{{ snowflake_private_key }}"


- name: Create database_role
  stevefulme1.snowflake.database_role:
    name: "{{ test_name }}"
    database_name: "{{ test_db }}"
    account: "{{ snowflake_account }}"
    user: "{{ snowflake_user }}"
    private_key: "{{ snowflake_private_key }}"
  register: create_result

- name: Verify creation
  ansible.builtin.assert:
    that:
      - create_result is changed

- name: Create database_role again (idempotency)
  stevefulme1.snowflake.database_role:
    name: "{{ test_name }}"
    database_name: "{{ test_db }}"
    account: "{{ snowflake_account }}"
    user: "{{ snowflake_user }}"
    private_key: "{{ snowflake_private_key }}"
  register: idem_result

- name: Verify idempotency
  ansible.builtin.assert:
    that:
      - idem_result is not changed

- name: Update database_role
  stevefulme1.snowflake.database_role:
    name: "{{ test_name }}"
    database_name: "{{ test_db }}"
    comment: "Updated role"
    account: "{{ snowflake_account }}"
    user: "{{ snowflake_user }}"
    private_key: "{{ snowflake_private_key }}"
  register: update_result

- name: Verify update
  ansible.builtin.assert:
    that:
      - update_result is changed

- name: Delete database_role
  stevefulme1.snowflake.database_role:
    name: "{{ test_name }}"
    database_name: "{{ test_db }}"
    state: absent
    account: "{{ snowflake_account }}"
    user: "{{ snowflake_user }}"
    private_key: "{{ snowflake_private_key }}"
  register: delete_result

- name: Verify deletion
  ansible.builtin.assert:
    that:
      - delete_result is changed

- name: Delete database_role again (idempotency)
  stevefulme1.snowflake.database_role:
    name: "{{ test_name }}"
    database_name: "{{ test_db }}"
    state: absent
    account: "{{ snowflake_account }}"
    user: "{{ snowflake_user }}"
    private_key: "{{ snowflake_private_key }}"
  register: delete_idem

- name: Verify delete idempotency
  ansible.builtin.assert:
    that:
      - delete_idem is not changed

- name: Drop test database
  stevefulme1.snowflake.database:
    name: "{{ test_db }}"
    state: absent
    account: "{{ snowflake_account }}"
    user: "{{ snowflake_user }}"
    private_key: "{{ snowflake_private_key }}"
